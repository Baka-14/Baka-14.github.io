services:
  jekyll:
    # Prebuilt image that matches the al-folio theme's Docker workflow.
    image: amirpourmand/al-folio:v0.14.6
    ports:
      - "8080:8080"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
    environment:
      JEKYLL_ENV: development
